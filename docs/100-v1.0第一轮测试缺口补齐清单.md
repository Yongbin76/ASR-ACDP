# v1.0 第一轮测试缺口补齐清单

> 口径说明
>
> 本文档中若出现 `主数据域 / 内容审核域` 等旧测试口径，当前统一按以下映射理解：
>
> - `主数据域` -> `词典建设 + 验证与回流`
> - `内容审核域` -> `词典审核`

## 1. 文档目的

本文档用于把 `JOB-090` 第一轮需要补齐的测试缺口固定下来。

目标不是一次性把测试做到极致，而是先补齐：

- 能稳定支撑 `v1.0`
- 能支撑 Codex 流水线执行
- 能支撑缺陷回流与修正闭环

## 2. P0 缺口

### 2.1 结果归档目录

- [x] 固定 `JOB-090` 测试结果归档目录
- [x] 固定批次命名规则
- [x] 固定 `summary.json` / `report.md` / `defects/` 结构

### 2.2 执行顺序

- [x] 固定测试执行批次顺序
- [x] 固定域级优先级
- [x] 固定测试 Codex / 开发 Codex / 人工协作边界

### 2.3 缺陷闭环

- [x] 固定测试报告模板
- [x] 固定缺陷回流模板
- [x] 固定开发 Codex 回传模板

### 2.4 外部条件 runbook

- [x] `JOB-006` 命令级 runbook
- [x] `JOB-009` 命令级 runbook
- [x] `JOB-030` 证据汇总 runbook

## 3. P1 缺口

### 3.1 覆盖矩阵补齐

- [x] 主数据域异常输入矩阵
- [x] 发布域回滚与发布后风险矩阵
- [x] 运行治理域异常分类矩阵
- [x] 系统配置域角色 / 权限 / 策略组合矩阵
- [x] 帮助域入口与 slug 绑定矩阵
- [x] 浏览器真实渲染完成度检查
- [x] 首页“当前用户 / 当前角色 / 工作台”加载完成检查
- [x] 页面启动异常采集与前端 console error 留档
- [x] 所有主要页面加载速度 / 首屏渲染耗时检查

### 3.2 数据准备清单落地

- [x] 测试账号与角色矩阵表
- [x] 主数据样本清单
- [x] 导入样本清单
- [x] validation case 清单
- [x] 节点备案样本清单

## 4. P2 缺口

### 4.1 已落地增强

- [x] 覆盖率门槛方案
- [x] contract snapshot 方案
- [x] 视觉回归方案

说明：

- 以上 3 项当前都已落地
- 但仍不建议把它们单独提升为 `v1.0` 硬门槛

## 5. 当前用途

本文档当前作为：

- `JOB-090` 第一轮测试补缺口清单
- 后续测试执行前的准备任务列表
