# 发布包与交付物详细设计

- 文档状态：active
- 适用版本：v1.0
- 文档类型：design
- 所属工作区：2026-04-17-v1.0正式文档工作区
- 最后更新时间：2026-04-18
- 责任对象：镜像、交付目录、发布说明包

## 1. 设计目标

把“运行镜像”“部署交付包”“发布说明包”边界彻底分清。

## 2. 当前交付物分类

### 2.1 运行镜像

1. `Dockerfile`
2. `Dockerfile.admin`
3. `Dockerfile.runtime`

### 2.2 部署交付包

模板目录：

1. `release_bundle_templates/deployment_bundle/`

### 2.3 发布说明包

模板目录：

1. `release_bundle_templates/release_notes_bundle/`

## 3. 打包边界

当前保留进正式镜像的文档仅限：

1. `docs/help_manuals/`
2. `docs/25-console宿主环境联调与smoke执行说明.md`
3. `docs/26-console内部试用说明.md`

不再整包复制 `docs/`。

## 4. 关键代码入口

1. [`prepare-release-bundle.js`](/Codex/ACDP/prototype/src/cli/prepare-release-bundle.js)
2. `Dockerfile*`
3. `.dockerignore`

## 5. 修改风险

1. 改镜像打包边界会影响交付物纯度。
2. 改 release bundle 模板会影响交付目录结构。

## 6. 相关测试

1. [`docker-packaging-boundary.test.js`](/Codex/ACDP/prototype/tests/unit/docker-packaging-boundary.test.js)
2. [`release-bundle-templates.test.js`](/Codex/ACDP/prototype/tests/unit/release-bundle-templates.test.js)
3. [`prepare-release-bundle.test.js`](/Codex/ACDP/prototype/tests/unit/prepare-release-bundle.test.js)
