# 页面手册：拼音审核

## 1. 页面目标

`拼音审核` 用于决定拼音候选是否进入词条拼音画像。

## 2. 统计口径

- 顶部卡片：显示拼音审核任务总量口径
- 顶部卡片：值为 0 的次级状态卡片默认不展示
- 结果区摘要：显示当前筛选结果口径
- 卡片列表：只显示当前页任务
- 分页：固定放在任务结果区块底部

## 3. 审核通过后

- 候选读音写入词条画像

## 4. 审核驳回后

- 返回词条详情继续治理
