# Console 收尾待办清单

## 1. 目的

本清单用于记录 `/console` 当前已确认但暂不继续处理的收尾问题，避免问题在后续联调和交接中丢失。

## 2. 当前待办

当前没有新的挂起项。

已在 `2026-04-04` 收口并移出的事项：

- `TODO-001` 发布审核 operator/self-review 交互歧义
  - `/console/reviews` 与 `/console/reviews/:taskId` 已补齐发布审核前端护栏
  - 当前前端会明确区分：
    - 提交人与审核人为同一操作人的阻断
    - 高风险双人审批中“同一审核人重复审批”的阻断
    - 仍需不同审核人继续审批的提示
  - 相关 release 详情页也已补齐审批进度与已通过审核人提示
  - 当前不再作为独立挂起项保留

## 3. 使用方式

后续继续 `/console` 收尾时，先检查本清单：

- 已修复的问题可移除或标记完成
- 新发现但暂不处理的问题，也统一追加到这里
