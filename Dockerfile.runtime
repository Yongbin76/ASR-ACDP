FROM node:22-bookworm-slim

WORKDIR /app

COPY package.json ./
COPY prototype ./prototype

RUN npm install --omit=dev

EXPOSE 8787

ENV ACDP_RUNTIME_PORT=8787

CMD ["sh", "-lc", "npm run start:runtime"]
